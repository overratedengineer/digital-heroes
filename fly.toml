# fly.toml app configuration for digital-heroes
# Configured for 24/7 always-on uptime (auto_stop_machines = false)

app = "digital-heroes"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[env]
  PORT = "4000"
  DATABASE_PATH = "/app/data/digital-heroes.db"
  NODE_ENV = "production"

[http_service]
  internal_port = 4000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1

[checks]
  [checks.health]
    port = 4000
    type = "http"
    interval = "20s"
    timeout = "5s"
    path = "/api/health"
    method = "GET"
