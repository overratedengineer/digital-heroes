import {FormEvent,useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {ArrowUpRight,HeartHandshake,LockKeyhole} from 'lucide-react';import {api,request} from '../lib/api';import {Button,Card,Toast} from '../components/ui';
export default function Auth({mode}:{mode:'login'|'signup'}){const nav=useNavigate();const signup=mode==='signup';const [form,setForm]=useState({name:'',email:'',password:'',charityId:'c1',charityPercent:10});const [err,setErr]=useState('');const [busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setErr('');try{const data:any=await request(()=>api.post(signup?'/auth/register':'/auth/login',form));localStorage.setItem('dh_token',data.token);localStorage.setItem('dh_name',data.user.name);localStorage.setItem('dh_role',data.user.role);nav('/dashboard')}catch(e:any){setErr(e.message)}finally{setBusy(false)}};return <div className="auth-page"><div className="auth-brand"><Link to="/" className="brand light"><span className="brand-mark">D</span><div>digital<span>heroes</span><small>PLAY · WIN · GIVE</small></div></Link></div><div className="auth-layout"><div className="auth-story"><span className="eyebrow">MEMBERSHIP, REIMAGINED</span><h1>Good games.<br/><em>Good causes.</em></h1><p>One membership connects your performance, a monthly reward engine and the cause you choose to support.</p><div className="auth-feature"><HeartHandshake/><div><b>Impact starts at 10%</b><span>Choose a charity and direct more whenever you want.</span></div></div><div className="auth-feature"><LockKeyhole/><div><b>Private by design</b><span>Your account and score history stay inside your member space.</span></div></div></div><Card className="auth-card"><span className="eyebrow">{signup?'CREATE YOUR MEMBERSHIP':'WELCOME BACK'}</span><h2>{signup?'Start playing with purpose.':'Good to see you again.'}</h2><p className="muted">{signup?'Create a free account first. You can activate a plan from your dashboard.':'Use the demo account below or your own member credentials.'}</p>{err&&<Toast message={err} type="error"/>}<form onSubmit={submit}>{signup&&<label>Name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Alex Morgan"/></label>}<label>Email<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@example.com"/></label><label>Password<input required type="password" minLength={8} value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="8+ characters"/></label>{signup&&<><label>Cause<select value={form.charityId} onChange={e=>setForm({...form,charityId:e.target.value})}><option value="c1">Fairway Futures Foundation</option><option value="c2">Green Steps Collective</option><option value="c3">Open Arms Health Fund</option><option value="c4">Next Chapter Education</option></select></label><label>Charity contribution <b>{form.charityPercent}%</b><input type="range" min="10" max="50" value={form.charityPercent} onChange={e=>setForm({...form,charityPercent:Number(e.target.value)})}/></label></>}<Button type="submit" disabled={busy}>{busy?'Creating…':signup?'Create account':'Log in'}</Button></form><div className="auth-demo"><b>Demo access</b><span>demo@digitalheroes.local · Demo@12345</span></div><p className="auth-switch">{signup?'Already a member?':'New here?'} <Link to={signup?'/login':'/signup'}>{signup?'Log in':'Create an account'} <ArrowUpRight size={13}/></Link></p></Card></div></div>}
