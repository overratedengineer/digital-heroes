import {ReactNode} from 'react'; import {motion} from 'framer-motion'; import {ArrowUpRight,CheckCircle2,AlertCircle,Loader2} from 'lucide-react';
export function Button({children,onClick,type='button',variant='primary',disabled=false,className='' }:{children:ReactNode,onClick?:()=>void,type?:'button'|'submit',variant?:'primary'|'secondary'|'ghost'|'danger',disabled?:boolean,className?:string}){return <button type={type} onClick={onClick} disabled={disabled} className={`btn btn-${variant} ${className}`}>{children}<ArrowUpRight size={16}/></button>}
export function Card({children,className='' }:{children:ReactNode,className?:string}){return <div className={`card ${className}`}>{children}</div>}
export function Stat({label,value,detail,accent=false}:{label:string,value:string|number,detail?:string,accent?:boolean}){return <Card className={accent?'stat-card accent':''}><span className="eyebrow">{label}</span><strong>{value}</strong>{detail&&<small>{detail}</small>}</Card>}
export function Badge({children,tone='neutral'}:{children:ReactNode,tone?:'neutral'|'success'|'warning'|'danger'|'mint'}){return <span className={`badge ${tone}`}><span className="dot"/>{children}</span>}
export function Toast({message,type='success'}:{message:string,type?:'success'|'error'}){return <motion.div initial={{y:20,opacity:0}} animate={{y:0,opacity:1}} className={`toast ${type}`}>{type==='success'?<CheckCircle2 size={18}/>:<AlertCircle size={18}/>} {message}</motion.div>}
export function Loading(){return <div className="loading"><Loader2 className="spin"/> Loading your experience…</div>}
