services:
  - type: web
    name: digital-heroes
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 4000
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_PATH
        value: /app/data/digital-heroes.db
